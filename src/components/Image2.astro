---
interface Props {
  name: string;
  ext: string;
  className?: string;
}
const { name, ext, className, aos } = Astro.props;
---

<picture class="picture">
  <source srcset=`/img/${name}@m.webp` type="image/webp" />
  <img src={`/img/amaury.jpg`} alt={name} />
</picture>
